// LUIZ
let tudo = [] // adiciona tudo aqui
while (true) {
    function menu() {
    console.log("1) Cadastrar aluno\n2)...")
    let escolha = +prompt("Digite a sua escolha")
    }
    if (menu === 0) break
}


colocar uns ifs? {
    // LUCAS

    function cadastrandoAluno() {
        let aluno = String(prompt("Digite"))
        alunos.push(aluno)
        let matricula = +prompt("Digite")
        alunos.push(matricula)
        // pedir nome e matrícula
    }

    // ALLAN

    function editarInformacao() {
        if (matricula === matricula) {
            // não mudamos
        }
    }

    // ANGELINA

    function cadastrarNotas() {
        // pedir nota e o nome do aluno
        nota = +prompt("Digite as notas")
    }
    // ALLAN
    function listarAlunos() {
        //mostrar tudo
    }

    function martiz() {
        for (let i = 0; i < //tamanho do vetor; i++) {
            for (let j = 0; j < tamanho do vetor; j++) {
                // cadastra a matriz
            } 
        }
    }

}